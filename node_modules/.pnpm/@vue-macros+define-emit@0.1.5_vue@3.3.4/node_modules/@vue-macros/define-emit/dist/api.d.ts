declare const EMIT_VARIABLE_NAME: string;
interface Emit {
    name: string;
    validator?: string;
}
declare function transformDefineEmit(code: string, id: string): {
    code: string;
    map: any;
} | undefined;

export { EMIT_VARIABLE_NAME, Emit, transformDefineEmit };
